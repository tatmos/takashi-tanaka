# ふるまいルール（AIたなかたかし）

AIたなかたかしの対話まわりで、よくある質問ボタンや同じ質問の繰り返しなど「ふるまい」の基準。definition/behavior.json と実装（app.js, chat-ui.js）はこのルールに合わせる。

---

## 1. よくある質問ボタン

### 1.1 押したボタンは残す

- よくある質問のボタンを押したあと、**押したボタンは消さず残す**。
- 押したボタンが何だったかわかるようにする（回答済みであることがわかるスタイルを付与してよい）。

### 1.2 別の質問を増やす

- ボタンを押したあと、**別の質問ボタンを 1 つ追加**する（プールから 1 つ出す）。
- 同じ質問が再度ボタンに出てもよい（何度も同じ質問を選べる）。

### 1.3 次の質問をしたら回答済みを消す

- ユーザーが**入力欄から次の質問を送信したとき**、**回答済みのボタン（押したボタン）をまとめて消す**。
- それ以外のボタンはそのまま残す。

---

## 2. 同じ質問の繰り返し

### 2.1 回数でふるまいを変える

- **同じ質問**（入力テキストが同じとみなしたもの）が繰り返されたとき：
  - **1 回目〜3 回目**: これまでどおり普通に答える。
  - **4 回目以降**: 答えずに**話をそらす**（別の話題を聞いてもらうような返答にする）。

### 2.2 話をそらすときの言い回し

- definition/behavior.json の `repeatDeflect` または `repeatDeflectPhrases` に書いた文を返す。
- **言い回しの例**（このあたりのトーンでそろえる）：
  - 「それ、何度か話したかも。別のことも聞いてみてください。」
  - 「もしかしたら、すでに話したかもです。履歴をみてもらえると幸い。」
- すでに話したかもしれないとやわらかく伝え、履歴を見てもらう・別の話題を促す言い回しを多めに使う。

### 2.3 話のそらし方の例（唐突に言う）

- 話をそらすとき、**唐突に**別のことを言うふるまいもあり得る。
- 例：「おなかすいた」「ねむいです」など、文脈と関係なく一言言ってから別の話題を促す、といったパターン。
- 実装：definition/behavior.json の `repeatDeflectPhrases` に複数書いておくと、話をそらすときに**ランダムに 1 件**選んで返す（未指定なら `repeatDeflect` のみ使用）。時間帯で選ぶ処理を足すとさらに楽しくなる。

### 2.4 時間帯で内容を変える（案）

- **時間帯**（朝・昼・夜など）によって、話をそらす文言やその他の返答のトーンが少し変わると楽しくなる。
- 例：朝は「ねむいです」、昼は「おなかすいた」、夜は「そろそろ寝るかも」など。
- 実装する場合は、definition/behavior.json に時間帯別の文言や、取得した時刻に応じて選ぶルールを追加する。

### 2.5 閾値

- 何回目から話をそらすかは definition/behavior.json の `repeatThreshold` で指定（既定は 4）。

---

## 3. 挨拶・不明な入力のオウム返し

### 3.1 挨拶はそのまま返す

- **挨拶**（こんにちは、こんにちわ、はじめまして、よろしく、おつかれ など）を言われたら、**そのままオウム返し**して返す。
- 例：「こんにちわ」と言われたら「こんにちわ」と返す。相手の反応を伺う。

### 3.2 挨拶っぽい・不明な単語もオウム返し

- **挨拶っぽいもの**や、**どのルールにもマッチしない不明な単語**も、そのまま**オウム返し**して、相手の反応を伺う。
- 実装：definition/rules.json の greeting ルールに `"echo": true` を付けると挨拶は入力そのまま返す。どのルールにもマッチしないとき（fallback）も、入力そのままを返す。

---

## 4. 年代別の答え（「最近は」）

### 4.1 接頭語「最近は」

- **年代別の答え**がある場合は、**「最近は」**のような接頭語をつけて、**ここ 1 年くらい**の内容で答えられるようにする。
- 例：「最近は Crowbar Climber や Guerrilla Gorilla とかに関わってます。」のように、直近の活動を伝える。

### 4.2 テンプレートでの使い方

- definition/timeline.json の `recentYear` などに「ここ 1 年」の要約や項目を用意し、ルールの template で `{{timeline.recentYear.summary}}` のように参照する。
- そのとき、文頭に「最近は」をつける（timeline 側に含めても、template 側で「最近は {{timeline.recentYear.summary}}」としてもよい）。

---

## 5. 年代別の情報源とデータ化

### 5.1 情報源

- **各 SNS の時刻タグ**（X の投稿日時など）を見て、いつ何をしていたかを把握する。
- **C:\Users\cha\Documents\web\** 以下にあるファイルの**日付的な情報**（HTML の `.date` や日付らしい文字列）と紐づけて、あらかじめデータ化する。

### 5.2 データ化の流れ

1. **C:\Users\cha\Documents\web\** 内の HTML などから、日付と内容を抽出する（手動またはスクリプト）。
2. **definition/timeline.json**（または definition/recent.json の拡張）に、**日付・ラベル・カテゴリ**などを構造化して格納する。
3. **ここ 1 年**に該当するものを `recentYear` にまとめ、返答のテンプレートから `{{timeline.recentYear.summary}}` で参照する。
4. **年ごと**の活動は `byYear` に年をキーにして格納する。各年は `summary` と `items`（label, date, category など）を持つ。ルールの template で `{{timeline.byYear.2024.summary}}` のように参照する（definition/rules.json に「2024年」「去年」などのキーワードでマッチするルールを追加する）。

### 5.3 SNS との連携

- SNS の時刻タグは、手動で timeline に反映するか、API などで取得してスクリプトで timeline.json を更新する想定。
- 現時点では手動で definition/timeline.json を更新し、必要に応じてスクリプトで web フォルダから日付情報を抽出する拡張を検討する。

---

## 6. 参照

- **定義**: definition/behavior.json（repeatThreshold, repeatDeflect など）、definition/rules.json（greeting の echo）、definition/timeline.json（年代別・ここ1年用）
- **実装**: js/app.js（getReply の繰り返し判定、createFaqButton、clearAnsweredFaqButtons）、js/rules-engine.js（echo と fallback 時のオウム返し）、js/chat-ui.js（bindSubmit の onAfterSubmit）
- **情報源**: 各 SNS の時刻タグ、C:\Users\cha\Documents\web\ 以下の日付情報 → timeline.json などにデータ化して参照
- **口調**: 言い回しルール.md
- **企画**: 企画書.md
