# 現状の動いているもので足りていないところ・改善案

現状の実装と定義を確認した結果。致命的な不具合はなく動くが、揃えておくとよい点・改善案をまとめる。

---

## 1. 軽微な UX・表示

### 1.1 リンク未設定の SNS が「〇〇は 。」と出る

- **状況**: リンクルール（サイト・X・Qiita…Facebook）の template で、`public.links.facebook` などが空文字のときも「Facebook は 。」とそのまま出る。
- **影響**: 回答が少し不自然。「Facebook は 。」だけが余る。
- **改善案**: 
  - 運用で、使わない SNS は template からいったん外す。
  - またはルールエンジン側で「値が空のプレースホルダーは文ごと出さない」などのルールを追加する（要仕様検討）。

### 1.2 定義の一部が読み込めないときのフォールバック

- **状況**: 例として `timeline.json` が 404 などで読めないと、`timeline.recentYear.summary` が空になり、「最近は  詳しくは…」のように途中が空になる。
- **影響**: 「最近は？」の回答が不完全になる。
- **改善案**: 
  - 必須の定義（basic, rules, timeline など）を決め、1つでも欠けたら「一部読み込めませんでした」と表示する。
  - または template 側で「値が空のときはこの文言」を指定できるようにする（要仕様検討）。

---

## 2. 定義は読み込んでいるが参照していないもの

- **tone.json**: 読み込みはしているが、自己紹介や返答の生成では未使用。言い回しルールや今後の「口調バリエーション」用。
- **params.json**: 読み込みはしているが、現状の rules の template では未参照。企画書の「確定パラメータ」用で、将来のルールで使う想定。

→ 現状動かすだけなら不要。活用するときは rules や renderSelfIntro から参照を足す。

---

## 3. 環境・運用まわり

### 3.1 file:// で開いたとき

- **状況**: `index.html` を直接開く（file://）と、`fetch("definition/...")` が CORS 等で失敗する環境がある。
- **影響**: 定義が読めず「読み込めなかったよ」や空の回答になりやすい。
- **改善案**: README や企画書に「GitHub Pages やローカルサーバ（例: `npx serve .`）で開いてください」と明記する。

### 3.2 GitHub Pages のベース URL

- **状況**: リポジトリ名で公開している場合、ベースが `/<repo-name>/` になる。`img/avatar.png` や `definition/*.json` は相対パスのため、そのままで動作する想定。
- **確認**: 実際に `https://<user>.github.io/<repo-name>/` で開いて、自己紹介・最近のこと・対話・アバター・フッターリンクが問題なく出るか確認すると安心。

---

## 4. アクセシビリティ・堅牢性（任意）

- **アバター画像**: `alt=""` で装飾扱いにしており、スクリーンリーダーではスキップされる。問題なし。
- **フォーム**: 入力欄に `<label for="chat-input">` と visually-hidden で対応済み。
- **履歴**: `aria-expanded` / `aria-controls` / `aria-live` 対応済み。
- **画像読み込み失敗**: `img/avatar.png` が 404 のとき、壊れたアイコンになる。必要なら `img` に `onerror` で非表示や代替表示を付けてもよい。

---

## 5. ドキュメントとの一致

- **企画書**: 画面構成・定義の役割・今後の拡張は現状と一致。
- **ふるまいルール・言い回しルール**: 実装（behavior.json, rules.json, 繰り返し時のそらし）と対応している。
- **faq.json の showCount**: 5 で、ボタン表示数と一致。

---

## 6. まとめ

| 種類           | 内容                                                                 |
|----------------|----------------------------------------------------------------------|
| 軽微な UX      | リンク未設定 SNS の「〇〇は 。」、定義欠損時の回答の空穴             |
| 未使用定義     | tone.json, params.json（将来用としてそのままでも可）                 |
| 環境・運用     | file:// では動かない場合あり→サーバで開く旨を README 等に明記       |
| 任意の強化     | アバター画像の onerror、必須定義チェック                            |

致命的な不足はなく、このまま運用できる。上記は「揃えておくとよりよい」程度の改善案。
