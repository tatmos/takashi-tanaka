# 企画書：AIたなかたかし

GitHub Pages で自己紹介・最近のこと・対話ができる 1 ページサイト（AIたなかたかし）にするための企画書。

---

## 1. 目的・ゴール

| 項目 | 内容 |
|------|------|
| **成果物** | ルートで公開する 1 ページサイト。自己紹介（定義から生成）・最近のこと（X タイムライン）・対話（AIたなかたかし）で、初めての人にも何者かわかるフレンドリーな構成。 |
| **公開** | GitHub Pages（Source: branch、Folder: **/ (root)**）。ベース URL は `/` または `/repo-name/`。静的ファイルの fetch は相対パスで行う。 |
| **技術** | 純 HTML/CSS/JS（Jekyll なし）。ミニマル・暗背景・読みやすさ優先。X タイムラインは公式 widgets.js で埋め込み。 |
| **対話の相手** | 主に自分本人の振り返り用。第三者も「この人」について質問できる。AIたなかたかしは定義に基づいて答える代弁役。 |

---

## 2. サイト構成（ルート公開）

GitHub Pages でルートから公開するため、エントリをプロジェクトルートに置く。

```
/
├── index.html            # エントリ（自己紹介・最近のこと・対話）
├── style.css
├── definition/           # 定義（カテゴリ別、大きくなりすぎないよう分割）
│   ├── basic.json        # 名前・一言・ハンドル
│   ├── profile.json      # 思考OS・報酬・動機・停止理由など
│   ├── params.json       # 確定パラメータ（対話から参照。画面には表示しない）
│   ├── public.json       # 公開してよいリンク・カテゴリ（park18 参照）
│   ├── recent.json       # 最近のこと（summary, twitterUrl。X タイムラインと併用）
│   ├── tone.json         # 口調（SNS @tatmos 分析後に更新）
│   ├── privacy.json      # 答えないキーワード（値は持たない）
│   └── rules.json        # Q&A ルール
├── js/
│   ├── definition-loader.js
│   ├── rules-engine.js
│   ├── chat-ui.js
│   └── app.js
├── README.md
├── 企画書.md
└── experiments/
```

- **個人情報**: 定義ファイルには公開してよい情報のみ。パスワード・住所・電話番号等は持たず、聞かれても答えない（privacy.json で拒否）。
- **公開範囲の基準**: 載せてよいものは [park18](http://park18.wakwak.com/~cha/index.html) に掲載されている情報を目安とする。それ以外は必要に応じて判断し、悪用されそうな情報は持たない・答えない。
- **口調**: tone.json で管理。SNS（[@tatmos](https://x.com/tatmos)）分析後に口調例を更新する。
- **定義の更新**: 現時点では手動で definition/*.json を編集する。UI からの編集は将来検討。
- **確定パラメータ**: definition/params.json は対話の返答から参照可能。画面には「確定パラメータ」セクションは表示しない。更新は手動で params.json を編集。
- **docs/**: ルート公開に移行済みのため docs/ は廃止してよい。旧版アーカイブが必要なら experiments/ など別途検討。

---

## 3. 自分という人間の定義

### 3.1 定義の役割

- アドバイザーAI や対話相手が「この人」を参照するための**唯一の仕様**として機能する。
- 対話の回答生成（ルールベース or AI）は、この定義をコンテキストとして使う。

### 3.2 定義の構造（案）

**A. フラットなキー・値（現行の暫定モデルを拡張）**

| キー | 説明 | 例 |
|------|------|-----|
| `type` | 思考OS・タイプ | 探索型R&Dエンジニア |
| `reward` | 主報酬 | 実装が現実で動いた瞬間 |
| `motive` | 開始動機 | 世界に存在しない構造を作りたい |
| `stopReason` | 停止理由 | 技術的に難しすぎる |
| `stopReasonDetail` | 難しさの内訳（確定パラメータ1） | 理論/時間/精神/スキル/環境 |
| `selfImage` | 自己認知 | 研究者・思想家寄り |
| `future10y` | 10年後 | 自由に面白い実験を続けたい |
| `others` | 他者の位置づけ（確定パラメータ2） | 観測者/共犯者/センサー/ノイズ |
| `emotionalFriction` | 感情摩擦トリガー（確定パラメータ3） | 短文リスト |
| `openness` | 公開・完了・フィードバック | 多い |
| `interruption` | 中断プロジェクト | 多い |
| `income` | 収益 | 主に労働型（給与） |
| `oneLiner` | 一言 | I seek new behaviors in reality. |

**B. データの置き場所**

- **definition/** … 上記をカテゴリ別に複数 JSON（basic, profile, params, public, recent, tone, privacy, rules）で保持。サイトと対話ロジックの両方から参照する。
- **README.md** … GitHub 用の人間読みテキスト。必要なら definition/ の内容を要約するか、手動で同期する。

### 3.3 定義の表示（現状の実装）

- **「定義」セクションは画面に置かない。** 代わりに以下を表示する。
- **たなかたかしについて（自己紹介）**: definition-loader で basic + profile を読み込み、app.js の renderSelfIntro でやわらかい自己紹介文を生成して表示。初めての人にも何者かわかるフレンドリーなトーンにする。
- **最近のこと**: definition/recent.json の summary を表示し、X のタイムラインを公式 widgets.js で埋め込み（@tatmos のツイートを自動表示）。recent.json で文言や URL を変更可能。
- **確定パラメータ**: 画面には表示しない。params.json は対話の返答生成時に参照可能。

---

## 4. インタラクティブ対話

### 4.1 対話の目的

- 訪問者（自分 or 第三者）が「定義」を前提に**質問・相談・検証**できるようにする。AIたなかたかしは定義に基づいて答える代弁役。
- 対話の結果を「定義の更新」に繋げる場合、現時点では手動で definition/*.json を編集する。対話ログのエクスポートや UI からの編集は将来検討。

### 4.2 対話の方式（選択肢）

| 方式 | 概要 | メリット | デメリット |
|------|------|----------|------------|
| **A. ルールベース** | キーワード・選択肢に応じて事前定義した返答を返す | 実装が簡単、API 不要、静的サイトのまま | 表現の幅が狭い、柔軟性は低い |
| **B. AI API** | 外部 AI API に「定義 + ユーザー発話」を渡して返答を生成 | 自然な対話、定義の解釈が柔軟 | API キー・課金・CORS 等の考慮が必要 |
| **C. ハイブリッド** | よくある質問はルール、それ以外は AI または「定義を表示して自分で考える」 | コストと柔軟性のバランスが取りやすい | 設計がやや複雑 |

**推奨（企画段階）:** まず **A. ルールベース** で対話 UI と「定義を参照した返答」の流れを作り、必要になったら B/C を検討する。

### 4.3 UI（対話部分・実装済み）

- **レイアウト:** ヘッダー → 自己紹介 → 最近のこと → 「なんでも聞いて」対話エリア → フッター。
- **入力:** 1 行テキスト + 送信ボタン。「よくある質問」ボタンは Phase 2 で検討。
- **出力:** 発話ごとに「あなた」「AIたなかたかし」の 2 列で履歴表示。システム返答は definition/ のデータと rules.json に基づく。
- **状態:** 履歴は現状メモリ上のみ（リロードで消える）。localStorage 永続化は他者利用時のプライバシーを考慮して検討する。

### 4.4 ルールベース対話のデータ構造

- **definition/rules.json** でトリガーと返答を定義する（app.js 内に持つことも可）。
  - トリガー: キーワード、正規表現、または「よくある質問」ボタンに紐づく ID。
  - 返答: 固定文 + definition/ の該当キーを埋め込んだ文（例: 「主報酬は {{profile.reward}} です」）。

---

## 5. 画面構成（ワイヤーイメージ・現状）

```
+------------------------------------------+
| AIたなかたかし                            |
| いろいろ聞いてね。気軽にどうぞ。            |
+------------------------------------------+
| たなかたかしについて                      |
| （basic + profile から生成した自己紹介）   |
+------------------------------------------+
| 最近のこと                                |
| （recent.json の summary + X タイムライン）|
+------------------------------------------+
| なんでも聞いて                            |
| [入力欄] [送信]                           |
| ---                                      |
| あなた: …                                 |
| AIたなかたかし: …（定義・ルールに基づく返答） |
+------------------------------------------+
| park18 · X @tatmos · Repository          |
+------------------------------------------+
```

---

## 6. 作業の順序

**現状:** 以下は実装済みである。

1. ルート公開（index.html / style.css / js/ をルートに配置）。GitHub Pages は Folder: / (root) で公開。
2. 定義の整備（definition/ をカテゴリ別に分割。basic, profile, params, public, recent, tone, privacy, rules）。definition-loader で読み込み。
3. 自己紹介表示（renderSelfIntro: basic + profile からやわらかい自己紹介文を生成）。
4. 最近のこと（recent.json の summary + X タイムライン埋め込み。widgets.js 使用）。
5. 対話 UI（入力・送信・履歴表示）。ルールベース返答（rules-engine + definition/rules.json）。params は対話から参照可能で、画面には表示しない。

**今後の拡張（案）:** よくある質問ボタン、履歴の永続化（要検討）、定義の UI 編集、AI API の検討。

---

## 7. 未確定・検討事項

- **対話の相手:** 主想定は 1 に記載済み（自分本人の振り返り＋第三者も質問可。AIたなかたかしは代弁役）。
- **対話の方式:** とりあえずルールベースでいく。AI API は将来検討。
- **言語:** 日本語のみでOK。英語は将来検討。
- **docs/:** 廃止済み（削除した）。旧版は experiments/ 等で必要なら検討。

---

## 8. まとめ

| 項目 | 内容 |
|------|------|
| **公開** | GitHub Pages、ルートに `index.html` を置く。ベース URL は / または /repo-name/。 |
| **構成** | 自己紹介（定義から生成）・最近のこと（recent.json + X タイムライン）・対話（AIたなかたかし）。確定パラメータ・Repo 運用ルールは画面には出さない。 |
| **定義** | definition/ 配下の JSON（カテゴリ別）でデータを保持。自己紹介の元ネタと対話の返答の共通ソース。 |
| **対話** | ルールベースで AIたなかたかし が定義を参照して返答。更新は手動で definition/* を編集。 |
| **現状** | 上記は実装済み。今後の拡張は 6 の「今後の拡張（案）」を参照。 |
