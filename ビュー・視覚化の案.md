# データビュー・視覚化の案

Q&A 以外に、定義データを**データ分析的に見るビュー**を用意する案。タイムラインの可視化や時期ごとの関連図の変化を見られるようにし、その情報を Q&A の解答にも活かす。

---

## 1. 想定する価値

- **タイムラインが見える** … いつ何をしていたかが一覧・グラフでわかる。自分や第三者の「見つめ直し」「客観情報」になる。
- **時期ごとの関連図の変化が見える** … ある年・ある期間に「何（ゲーム・イベント・コミュニティ）とつながっていたか」が図でわかる。時期を変えると関連がどう変わったかが比較できる。
- **ビューを参考に Q&A できる** … 同じ definition（timeline.json, public.json など）を参照しているので、ビューで見ている情報＝対話で答えられる情報になる。「この時期は？」「このつながりは？」と質問し、解答に反映できる。

---

## 2. ビューの種類（案）

### 2.1 タイムラインビュー

- **データ元**: definition/timeline.json（recentYear, byYear, items）。
- **表示イメージ**:
  - 年軸（横または縦）に沿って、ゲーム・CEDEC・イベント・ライブなどの項目を並べる。
  - 各項目はラベル・日付・カテゴリ（game / cedec / event / live）を持ち、色分けやフィルタで絞れる。
  - 「最近1年」を強調したり、年をクリックするとその年の summary や items の詳細を出したりする。
- **用途**: 活動の流れの確認、自分史の整理、第三者への説明材料。Q&A の「最近は」「〇〇年は」の答えの裏付けとして参照できる。

### 2.2 時期ごとの関連図ビュー

- **データ元**: definition/timeline.json の byYear、definition/public.json の links・categories。必要に応じて「誰／何とつながっているか」を items に label + category で持つ。
- **表示イメージ**:
  - **時期セレクタ**: 年または期間（例：2020年、2018–2020年）を選ぶ。
  - **関連図**: 選んだ時期の items をノードとして表示。カテゴリ（ゲーム・イベント・CEDEC・コミュニティなど）ごとにグループ化または色分け。ノード間の線は「同じ年に登場」「同じカテゴリ」などでつなぐ（シンプルなグラフ）。
  - **時期を変える**: セレクタで年を変えると、その年だけのノード・辺に切り替わり、「時期ごとの関連の変化」が比較できる。
- **用途**: ある時期に何に集中していたか、どのコミュニティ・イベントとつながっていたかの視覚化。Q&A で「その頃は〇〇と△△に関わってました」と答えるときの根拠になる。

### 2.3 リンク・コミュニティ一覧ビュー（簡易）

- **データ元**: definition/public.json（links, highlights, categories）。
- **表示イメージ**: リンクをカテゴリ別に一覧表示。SNS・イベント・リポジトリなどを「見える化」する。現在のフッターの拡張版。
- **用途**: 関連するコミュニティ・リソースの整理。Q&A の「サイトは」「イベントは」の答えと一致させる。

---

## 3. Q&A との連携

- **共通データ**: ビューも Q&A も同じ definition（timeline.json, public.json, rules.json など）を参照する。
- **ビューで見た情報を解答に**: ルールの template ではすでに `{{timeline.recentYear.summary}}` や `{{timeline.byYear.2024.summary}}` を参照している。ビューは同じデータの「見える形」なので、ビューで確認した内容がそのまま対話の返答と対応する。
- **「この時期は？」「このつながりは？」**: ビューで時期や関連を確認しつつ、対話で「2024年は？」「サウンドミニハッカソンとは？」と聞くと、同じ definition から答える。必要なら rules に「〇〇年の関連」「〇〇コミュニティ」用のキーワード・template を追加し、ビューで見える項目を解答で言及できるようにする。

---

## 4. 実装の置き場所・進め方

- **実験**: ビューはまず experiments/ 以下に置く（例: experiments/YYYYMM-timeline-view, experiments/YYYYMM-related-graph）。repo 運用ルールに従う。
- **技術**: 静的 HTML/JS で、definition/ の JSON を fetch して描画。タイムラインはリスト＋簡易タイム軸、関連図はノード・辺を SVG または Canvas で描くか、ライブラリ（D3.js など）を検討。
- **本サイトへの組み込み**: 実験が固まったら、index.html から「タイムラインを見る」「関連図を見る」のようなリンクでビューページへ飛ばす、または同一ページ内に折りたたみセクションで組み込むかを検討する。

---

## 5. 参照

- **定義**: definition/timeline.json（年代別・最近）、definition/public.json（リンク・カテゴリ）
- **企画**: 企画書.md 1.1（目標：客観情報・コミュニティの視覚化）、6 節（今後の拡張）
- **運用**: .cursor/rules/repo.mdc（experiments/ の置き場所）
